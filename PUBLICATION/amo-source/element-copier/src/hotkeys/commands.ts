/** Manifest `commands` ids — names must stay stable across releases. */
export const COMMAND_EXECUTE_ACTION = "_execute_action";
export const COMMAND_ACTIVATE_DEACTIVATE = "activate-deactivate";

/** Second key in catalog prefix mode for Element Copier. */
export const PREFIX_ACTION_KEY = "C";
